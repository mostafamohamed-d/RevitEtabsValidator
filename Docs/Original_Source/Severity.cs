namespace RevitEtabsValidator.Core.Validation
{
    // RECONSTRUCTED FROM USAGE (ColumnComparer: Info, Warning, Error, Critical).
    public enum Severity
    {
        Info,
        Warning,
        Error,
        Critical
    }
}
