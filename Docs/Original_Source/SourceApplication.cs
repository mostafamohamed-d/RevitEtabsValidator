namespace RevitEtabsValidator.Core.Enums
{
    // RECONSTRUCTED FROM USAGE (RevitColumnReader.Source = SourceApplication.Revit,
    // EtabsColumnReader.Source = SourceApplication.Etabs). Diff against your real
    // file — if you have more members (e.g. Safe already stubbed in), keep them.
    public enum SourceApplication
    {
        Revit,
        Etabs
    }
}
